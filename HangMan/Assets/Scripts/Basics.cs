using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Basics : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //string myName = "AYSU";
        //Debug.Log(myName.Substring(0));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
